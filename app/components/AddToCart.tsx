import React from 'react'

function AddToCart() {
    return (
        <div>
            <button onClick={() => alert("Click on Add to Cart")}>Add to cart</button>
        </div>
    )
}

export default AddToCart
